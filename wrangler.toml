# ============================================================
# BALI CATERING SYSTEM — Cloudflare Pages config
# Frontend estático (Vite build) + Supabase como backend
# O backend é o Supabase (Auth + Database + Storage)
# ============================================================

name = "baliservices"
compatibility_date = "2026-09-01"

# Pasta de build estático para Pages
pages_build_output_dir = "dist"
